# MLX-Audio TTS Dependencies for Kokoro-82M
# ==========================================
#
# Installation: pip install -r requirements-mlx-audio.txt
#
# Note: Model download (~500MB) happens on first synthesis

# MLX-Audio Package (will pull correct dependencies)
mlx-audio>=0.3.1

# Audio Processing
soundfile>=0.12.1

# Text Processing (for Kokoro phonemization)
# CRITICAL: misaki 0.8.x required - 0.9.x has compatibility bugs
misaki>=0.8.0,<0.9.0
num2words>=0.5.12

# Optional: spacy model for better text processing
# Run after install: python -m spacy download en_core_web_sm
