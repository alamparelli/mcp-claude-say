# Claude-Say & Claude-Listen Dependencies

# MCP Framework
mcp>=1.0.0

# Audio capture (for claude-listen)
sounddevice
numpy
soundfile

# Speech-to-Text (Parakeet-MLX - fast STT for Apple Silicon)
parakeet-mlx

# Global hotkey detection (for push-to-talk)
pynput
