# Claude-Say & Claude-Listen Dependencies

# MCP Framework
mcp>=1.0.0

# Audio capture (for claude-listen)
sounddevice
numpy
soundfile

# VAD - Voice Activity Detection (Silero via ONNX)
# onnxruntime v1.23+ includes CoreML EP for Apple Silicon Neural Engine
onnxruntime>=1.23.0

# Speech-to-Text (for claude-listen)
parakeet-mlx  # Fast STT for Apple Silicon via MLX
